# 🌞 Simple Landing Page

This is a **basic landing page** built using **HTML** with **inline CSS** (no external stylesheet).  
It demonstrates how to design a simple webpage layout that includes a **hero section** and an **about section** — perfect for beginners learning HTML and CSS fundamentals.

---

## 🎨 Features

- Full-width blue **hero banner** section at the top  
- Centered **heading**, **subheading**, and **call-to-action button**  
- Clean **About Us** section below the banner  
- Uses only inline CSS styles (all styling done within the HTML file)  

---

## 🧩 Code Explanation

- The top `<div>` uses `background-color: #3498db;` and `color: white;` to create the blue banner.  
- `padding: 80px 20px;` adds vertical spacing around the content inside the banner.  
- The white “Get Started” button is created using a `<button>` tag with inline styles for color, border radius, and padding.  
- The second `<div>` represents the **About Us** section, using a neutral background and centered text.  
- `text-align: center;` ensures all content stays centered across the page.  

---
